# Public build-time config for the SPA (the publishable key is safe to commit).
VITE_SUPABASE_URL=https://kmlheefyzhhegxmtaovq.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_9R9LMwYGu14evgtEJd4J3Q_CHhDbQsI
# Sentry's browser DSN is public by construction — it ships inside the bundle
# whatever we do with it — so it belongs here rather than in Vercel's dashboard,
# where it would be one more thing to remember on a redeploy. It grants nothing
# but "may post events to this project"; the key carries a server-side rate
# limit for that reason. Its absence is the off switch: no DSN, no SDK.
VITE_SENTRY_DSN=https://196bda5cae95c8386b6f97644a80e50f@o4512011830886400.ingest.de.sentry.io/4512011885019216
